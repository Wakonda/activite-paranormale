<div id="tab_resources" class="d-flex justify-content-between flex-wrap mt-2">
	<a href="{{ path('News_World') }}" class="btn btn-outline-primary position-relative rounded-pill {{ 'News_World' == route ? 'active' : '' }}">{{ 'news.index.Actualite'|trans([], 'validators') }}<span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">{{ counter.news > 999 ? "+999" : counter.news }}</span></a>
	<a href="{{ path('Biography_World') }}" class="btn btn-outline-primary position-relative rounded-pill {{ 'Biography_World' == route ? 'active' : '' }}">{{ 'biography.index.Biography'|trans([], 'validators') }}<span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">{{ counter.biography > 999 ? "+999" : counter.biography }}</span></a>
	<a href="{{ path('Book_World') }}" class="btn btn-outline-primary position-relative rounded-pill {{ 'Book_World' == route ? 'active' : '' }}">{{ 'book.index.Book'|trans([], 'validators') }}<span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">{{ counter.book > 999 ? "+999" : counter.book }}</span></a>
	<a href="{{ path('Cartography_World') }}" class="btn btn-outline-primary position-relative rounded-pill {{ 'Cartography_World' == route ? 'active' : '' }}">{{ 'cartography.index.Cartography'|trans([], 'validators') }}<span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">{{ counter.cartography > 999 ? "+999" : counter.cartography }}</span></a>
	<a href="{{ path('EventMessage_World') }}" class="btn btn-outline-primary position-relative rounded-pill {{ 'EventMessage_World' == route ? 'active' : '' }}">{{ 'eventMessage.index.Event'|trans([], 'validators') }}<span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">{{ counter.eventMessage > 999 ? "+999" : counter.eventMessage }}</span></a>
	<a href="{{ path('Photo_World') }}" class="btn btn-outline-primary position-relative rounded-pill {{ 'Photo_World' == route ? 'active' : '' }}">{{ 'photo.index.Photo'|trans([], 'validators') }}<span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">{{ counter.photo > 999 ? "+999" : counter.photo }}</span></a>
	<a href="{{ path('Video_World') }}" class="btn btn-outline-primary position-relative rounded-pill {{ 'Video_World' == route ? 'active' : '' }}">{{ 'video.index.Video'|trans([], 'validators') }}<span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">{{ counter.video > 999 ? "+999" : counter.video }}</span></a>
	<a href="{{ path('Witchcraft_World') }}" class="btn btn-outline-primary position-relative rounded-pill {{ 'Witchcraft_World' == route ? 'active' : '' }}">{{ 'witchcraft.index.Witchcraft'|trans([], 'validators') }}<span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">{{ counter.witchcraft > 999 ? "+999" : counter.witchcraft }}</span></a>
</div>