{% extends "admin/GenericAdmin/show.html.twig" %}

{% block h2_title %}{{ "admin.index.SEODirectory"|trans([], "validators") }}{% endblock %}

{% block edit_admin_route %}{{ path('WebDirectorySEO_Admin_Edit', { 'id': entity.id }) }}{% endblock %}
{% block new_admin_route %}{{ path('WebDirectorySEO_Admin_New') }}{% endblock %}
{% block index_admin_route %}{{ path('WebDirectorySEO_Admin_Index') }}{% endblock %}
{% block delete_admin_route %}{{ path('WebDirectorySEO_Admin_Delete', { 'id': entity.id }) }}{% endblock %}

{% block tbody_admin_table %}
	<div class="form-table-cell form-table-cell-head">Id</div>
	<div class="form-table-cell">{{ entity.id }}</div>

	<div class="form-table-cell form-table-cell-head">{{ "directorySEO.admin.ReturnLink"|trans([], "validators") }}</div>
	<div class="form-table-cell">{{ entity.returnLink|raw }}</div>

	<div class="form-table-cell form-table-cell-head">{{ "directorySEO.admin.Title"|trans([], "validators") }}</div>
	<div class="form-table-cell">{{ entity.title }}</div>

	<div class="form-table-cell form-table-cell-head">{{ "directorySEO.admin.Text"|trans([], "validators") }}</div>
	<div class="form-table-cell">{{ entity.text }}</div>

	<div class="form-table-cell form-table-cell-head">{{ "directorySEO.admin.Link"|trans([], "validators") }}</div>
	<div class="form-table-cell">{{ entity.link }}</div>

	<div class="form-table-cell form-table-cell-head">{{ "directorySEO.admin.Language"|trans([], "validators") }}</div>
	<div class="form-table-cell"><img src="{{ asset(entity.language.getAssetImagePath()) }}{{ entity.language.logo }}" alt="" width="20" height="13" /></div>
{% endblock %}