<h1>Contact creation</h1>

{{ form_start(form, {'action': path('contact_create'), 'attr':{'class' : 'form-horizontal', novalidate: 'novalidate'}}) }}
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
{{ form_end(form) }}

<ul class="record_actions">
    <li>
        <a href="{{ path('contact') }}">
            Back to the list
        </a>
    </li>
</ul>
