<script src="{{ asset('extended/js/DataTables/media/js/jquery.dataTables.js') }}"></script>
<link rel="stylesheet" href="{{ asset('extended/js/DataTables/media/css/jquery.dataTables.css') }}" type="text/css" media="screen" />
<script>
	document.querySelector(".musicContainer a.file_link").addEventListener("click", (e) => {
		e.preventDefault();
		parent.$(".musicContainer input.music_piece_file").val("");
		parent.$(".musicContainer input.existing_photo_colorbox").val(e.target.innerHTML);
		$('.selectFileModal').modal('hide');
		return false;
	});

	$(function()
	{
		$('table.index_admin').dataTable({
			"bFilter": false,
			"bInfo": false,
			"bPaginate": false,
		});
	}); 
</script>
<h2 class="title"><b>Choose an existing file</b></h2>
<table class="table table-striped table-bordered index_admin">
	<thead>
		<tr>
			<th>File</th>
		</tr>
	</thead>
	<tbody>
		{% for file in filesArray %}
			<tr>
				<td><a href="" alt="file" class="file_link">{{ file }}</a></td>
			</tr>
		{% endfor %}
	</tbody>
</table>