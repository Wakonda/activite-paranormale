<p class="image"><img class="image_wrapper mx-auto d-block img-fluid" src="{{ asset('extended/photo/' ~ family ~ '.webp') }}" alt="Woman writting poems" width="550" /></p>
<hr>
{% include 'quotation/Quotation/_search.html.twig' with { route: 'Poem_listDatatables' } %}

<table class="doc_datatables dt-responsive w-100">
	<thead>
		<tr>
			<th class="label_form d-none"></th>
			<th class="label_form">{{ "quotation.list.Poem"|trans([], "validators") }}</th>
			<th class="label_form">{{ "quotation.list.Author"|trans([], "validators") }}</th>
			<th class="label_form">{{ "quotation.list.Read"|trans([], "validators") }}</th>
		</tr>
	</thead>
	<tbody>

	</tbody>
	<tfoot>
		<tr>
			<th class="label_form d-none"></th>
			<th class="label_form">{{ "quotation.list.Poem"|trans([], "validators") }}</th>
			<th class="label_form">{{ "quotation.list.Author"|trans([], "validators") }}</th>
			<th class="label_form">{{ "quotation.list.Read"|trans([], "validators") }}</th>
		</tr>
	</tfoot>
</table>