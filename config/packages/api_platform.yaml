api_platform:
    mapping:
        paths:
            - '%kernel.project_dir%/src/Entity' # default configuration for annotations
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]
    collection:
        pagination:
            items_per_page: 12 # Default value
